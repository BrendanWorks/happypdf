# Pinned to the exact versions the vertical slice was tested against.
# (These supersede the draft pins in the setup spec; modal 0.67 predates the
#  Function.from_name API the orchestrator uses, so the tested 1.x pin is required.)

playwright==1.60.0     # headless Chromium driver for running axe-core in a real browser
PyMuPDF==1.27.2.3      # PDF parsing + embedded image extraction (imported as `fitz`)
modal==1.4.3           # calls the deployed olmOCR and Qwen2-VL GPU functions
# NOTE: olmocr itself is NOT a local dependency — it (and its torch/vllm stack)
# is installed only inside the Modal GPU image. Do not add it here or CI will try
# to install multi-GB GPU wheels. The extraction model is pinned in the image:
#   production: modal/modal_olmocr_final.py  (olmocr unpinned -> CLI default, v1)
#   staging:    modal/modal_olmocr_v2.py     (olmocr>=0.4.0, --model olmOCR-2-7B-1025-FP8)
requests>=2.31.0       # HTTP utility for hosted-mode model backends (future rounds)
anthropic==0.111.0     # Claude judge (Opus 4.8) — synthesizes peer reviews into patches
beautifulsoup4==4.14.3 # judge HTML parsing (data-ir-id -> element index)
lxml==6.1.1            # applicator: fast, lenient HTML tree mutation for patches
openai==1.109.1        # live GPT reviewer
google-genai==1.75.0   # live Gemini reviewer (the unified SDK; not google-generativeai)
httpx==0.28.1          # async calls to the OLMo Modal endpoint
fastapi==0.138.0       # demo API in front of the pipeline (api/main.py)
uvicorn==0.49.0        # ASGI server for the demo API
python-multipart==0.0.32  # PDF upload handling for live jobs

# Testing
pytest>=7.0.0          # testing framework
pytest-asyncio>=0.21.0 # async test support
